import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';  // Si besoin pour directives

@Component({
  selector: 'app-exit-voucher',
  standalone: false,
  templateUrl: './exit-voucher.component.html',
  styleUrls: ['./exit-voucher.component.scss']
})
export class ExitVoucherComponent {
  // Logique (injectez services e.g., constructor(private service: ...) {})
}
