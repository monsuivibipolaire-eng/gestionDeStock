import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';  // Si besoin pour directives

@Component({
  selector: 'app-entry-voucher',
  standalone: false,
  templateUrl: './entry-voucher.component.html',
  styleUrls: ['./entry-voucher.component.scss']
})
export class EntryVoucherComponent {
  // Logique (injectez services e.g., constructor(private service: ...) {})
}
